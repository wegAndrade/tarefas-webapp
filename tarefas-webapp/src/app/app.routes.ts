import { Routes } from '@angular/router';
import {TarefaListComponent } from './Components/tarefa-list/tarefa-list.component';

export const routes: Routes = [
  { path: 'lista-tarefas', component: TarefaListComponent }
];
